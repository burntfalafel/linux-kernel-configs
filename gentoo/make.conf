# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-march=haswell -O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
MAKEOPTS="-j3"

USE="-kde -systemd -gnome -aqua -cdinstall -cdr -css -dvd -dvdr -a52 -cjk \
     -clamav -coreaudio -ios -ipod -iee1394 -emacs -xemacs \
     -emboss -3dfx -altivec -smartcard -cups -ibm elogind dispatch-conf \
     dbus -llvm_targets_AMDGPU -llvm_targets_NVPTX alsa X xinerama \
     savedconfig acpi bluetooth -wayland cli libmpv networkmanager vulkan"
#RUBY_TARGETS="ruby24 ruby25"
# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

GENTOO_MIRRORS="ftp://ftp.free.fr/mirrors/ftp.gentoo.org/ ftp://mirror.bytemark.co.uk/gentoo/ ftp://mirrors.gethosted.online/gentoo"
GRUB_PLATFORMS="efi-64"
INPUT_DEVICES="mouse keyboard evdev libinput synaptics"
VIDEO_CARDS="intel i965"
ACCEPT_KEYWORDS="~amd64"
